[project]
name = "temoa-web-gui"
version = "0.1.0"
description = "Web interface for Temoa Energy System Model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "temoa>=4.0.0a1",
    "fastapi",
    "uvicorn[standard]",
    "tomlkit",
    "datasette",
    "websockets",
]
